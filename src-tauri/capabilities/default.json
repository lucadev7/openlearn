{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the OpenLearn main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
